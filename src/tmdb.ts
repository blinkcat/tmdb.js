import { account, certifications, changes, company, discover, movies } from './api';

//   private sessionId: string;
//   private guestSessionId: string;

const TMDB = {
  account,
  certifications,
  changes,
  company,
  discover,
  movies
};

export default TMDB;
